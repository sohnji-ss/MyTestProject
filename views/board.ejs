<!DOCTYPE html>
<html>
<head>
  <title>WHO 전북대학교</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <% include static/js.ejs%>
  <% include static/css.ejs%>
    <%include header.ejs %>
  <style>
  body{
  padding-top: 70px;
  }

  </style>
</head>
<body>

<div class="container">
  <div class="row">
    <div class="thumbnails">
      <% for(var i=0; i<6; ++i) {%>
      <% if (!rows[i]) break; %>
      <div class="col-md-4">
        <div class="thumbnail">
          <img class= "portrait" src="http://localhost:3000/<%= rows[i].img_url %>" alt="ALT NAME">
          <div class="caption">
            <h3><%= rows[i].title %></h3>
            <p><%= rows[i].description %></p>
            <p align="center"><a href=javascript:alert('버튼누름'); class="btn btn-primary btn-block">상세보기</a></p>
          </div>
        </div>
      </div>
      <% } %>
    </div>
  </div>
  <a class="btn btn-default">글쓰기</a>
  <div class="text-center">
    <!--<ul class="pagination">
      <li class="active"><a href="#"><span class="glyphicon glyphicon-chevron-left"></span></a></li>
      <li><a href="#">1</a></li>
      <li><a href="#">2</a></li>
      <li><a href="#">3</a></li>
      <li><a href="#">4</a></li>
      <li><a href="#">5</a></li>
      <li class="disabled"><a href="#"><span class="glyphicon glyphicon-chevron-right"></span></a></li>
    </ul>-->
    <ul class="pagination">
      <%if(currentPage == 1) { %>
      <li class="disabled"><span class="glyphicon glyphicon-chevron-left"></span></li>
      <% }else{%>
      <li> <a href="/board/?page=<%= +currentPage -1%>"><span class="glyphicon glyphicon-chevron-left"></span></a></li>
          <% }%>
        <%
        var i = 0;
        for (i = currentPage; i< +currentPage+pageCount; i++) { %>
          <%= i%>
          <% if (currentPage == i) { %>
            <li class="active"><span><%= i %> <span class="sr-only">(current)</span></span></li>
          <% } else { %>
              <li><a href="/board/?page=<%= i %>"><%= i %></a></li>
        <% } %>
      <% } %>
      <%if(currentPage == maxPage) { %>
      <li class="disabled"><span class="glyphicon glyphicon-chevron-right"></span></li>
        <% }else{%>
      <li> <a href="/board/?page=<%= +currentPage +1%>"><span class="glyphicon glyphicon-chevron-right"></span></a></li>
        <% }%>
    </ul>




  </div>
</div>
      <% include footer.ejs %>
</body>
</html>